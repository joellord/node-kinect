"use strict";
import assert from "assert";
import chai from "chai";

chai.should();

describe("kinect-gestures", function () {

  it("should have unit test!", function () {
    assert(true, true);
  });

  it("you will need to get up and move in front of your kinect !", function() {
    assert(true, true);
  });

});
